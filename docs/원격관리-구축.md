# 원격 관리·장애 대응 현황과 보완

> 작성 2026-08-03 · **2026-08-04 전면 개정** (실제 장애를 겪고 서버를 실측해 원인이 확정됨)
>
> ⚠️ 초판에는 추정으로 쓴 내용이 있었고 **상당 부분이 틀렸다.** 실제로는 감시·자동복구·
> 텔레그램 제어가 이미 잘 구축돼 있었다. 이 문서는 실측 결과로 다시 쓴 것이다.

---

## 1. 실제로 일어난 일 (2026-08-03 ~ 08-04)

**원인: 서버 PC의 랜선이 물리적으로 빠져 있었다.** 사용자가 발견해 다시 꽂자 복구됐다.

증상의 연쇄는 이렇다.

```
랜선 분리
  → 게이트웨이(192.168.2.1) 도달 불가
  → DNS 조회 실패  "lookup _v2-origintunneld._tcp.argotunnel.com ... i/o timeout"
  → cloudflared 가 Cloudflare 엣지 주소를 못 찾아 스스로 종료
  → Docker 가 재시작(restart=always) → 또 DNS 실패 → 반복
  → 전 도메인 530 (error code 1033)
```

| 항목 | 값 |
|---|---|
| 장애 구간 | **2026-08-03 10:39 ~ 08-04 09:08 (약 22.5시간)** |
| 복구 방식 | **자동.** 케이블 재연결 후 09:08:38 에 터널 4개 연결이 스스로 등록됨 |
| 서버 재부팅 | 없음 (uptime 175일 유지) |
| 미발송 알림 | **84건** (`~/monitoring/undelivered_alerts.log`) |

---

## 2. 잘 동작한 것 — 손댈 필요 없다

초판에서 "해야 한다"고 적었던 것들은 **이미 되어 있었다.**

### 재시작 정책 (초판 기술 오류)
전 컨테이너 13개에 `always` 또는 `unless-stopped` 가 **이미 설정돼 있다.**
초판의 "`no` 나 빈 값이면 바꿔야 한다"는 서술은 확인 없이 쓴 추정이었고 사실과 다르다.

```bash
# 확인 명령 (참고용)
for c in $(docker ps -a --format '{{.Names}}'); do
  printf '%-26s %s\n' "$c" "$(docker inspect $c --format '{{.HostConfig.RestartPolicy.Name}}')"
done
```

### 자동 복구
회선이 돌아오자 **사람 개입 없이** 터널이 스스로 복구됐다. Docker 재시작 정책이 의도대로 작동한다.

### 감시 시스템 — `~/monitoring/` (2026-07-28 구축)
초판에서 제안했던 watchdog 스크립트보다 **훨씬 정교한 것이 이미 있다.** 새로 만들 필요 없다.

| 파일 | 주기 | 역할 |
|---|---|---|
| `watchdog.sh` | 1분 | 내부·외부 프로브, 원인 판정, 안전 재시작 |
| `control.sh` | 1분(롱폴링 40초) | 텔레그램 원격 제어 (`/status`·`/incidents`·`/pause`·`/resume`) |
| `report.sh` | 10분 | 장애 대시보드 HTML 갱신 |
| `lib.sh` | — | 프로브·알림·쿨다운·인시던트 공통 라이브러리 |
| `config.env` | — | 설정(권한 0600, 텔레그램 토큰 포함) |

갖춰진 안전장치:
- `000`(측정 실패)은 재시도 후에도 실패해야 인정
- `FAIL_THRESHOLD=3` 회 연속 실패해야 조치 (1회로는 아무것도 안 함)
- 대상별 재시작 쿨다운(터널 600초)
- **서킷 브레이커** — 1시간에 4회 초과 재시작 시 자동조치 중단. **롤링 윈도우라 스스로 풀린다**
- 결과를 전부 모은 뒤 원인을 **한 번만** 판정 (개별 반응 금지)
- 인시던트별 증거 디렉터리 자동 생성

> 이 구조는 2026-07-28 새벽, 스크립트 3개가 각자 터널을 재시작하며 서로의 헬스체크를
> 깨뜨린 3시간 폭주를 겪고 만들어진 것이다. **공유 자원은 반드시 한 곳에서만 조작해야 한다.**

### 텔레그램 봇
**봇은 죽지 않았다.** 감지도, 재시작 시도도, 인시던트 기록도 전부 정상이었다.
다만 인터넷이 끊겨 `api.telegram.org` 에 도달할 수 없어 발송만 실패했고,
알림은 `undelivered_alerts.log` 에 보존됐다. 회선 복구 후 `/status`·`/resume` 정상 수신 확인.

---

## 3. 고친 것 — watchdog 이 회선 장애를 터널 장애로 오판했다

### 문제

`watchdog.sh` 의 판정 로직이 이랬다.

```
외부 전부 실패 + 내부 전부 정상  →  NPM 이 정상이면 "터널 문제"  →  터널 재시작
```

**랜선이 빠져도 증상이 정확히 이것이다.** 그래서 `"NPM 은 정상이나 외부 전체 불가 -> 터널 문제"`
로 판정하고 **22시간 동안 85회 재시작**했다. 회선이 없으면 터널은 뜰 수 없으므로 전부 무의미했고,
인시던트 기록에도 잘못된 원인이 남았다.

### 조치 (2026-08-04 적용 완료)

`lib.sh` 에 `uplink_ok()` 를 추가하고, `watchdog.sh` 가 **터널/NPM 을 정하기 전에** 호출하게 했다.
업링크가 죽었으면 재시작하지 않고 정확한 사유로 알림만 남긴다(회선 복구 시 발송된다).

확인 순서 — 좁은 것부터:

1. **기본 경로·게이트웨이 ping** → 랜선 분리·공유기 다운
2. **공인망 도달(`https://1.1.1.1/`, DNS 안 탐)** → 공유기는 살아있고 ISP 회선만 죽은 경우
3. **DNS 조회(`cloudflare.com`)** → 회선은 살아도 리졸버가 죽은 경우
   (cloudflared 는 SRV 조회에 실패하면 스스로 종료하므로 이게 곧 터널 다운이다)

검증 결과 — 문법검사 통과 / 정상 상태 `반환=0` / 게이트웨이 무응답 시뮬 `반환=1, 사유="게이트웨이
무응답 - 랜선 분리 또는 공유기 다운"` / 수동 실행 종료코드 0 / cron 오류 없음.

수정 전 원본은 `~/monitoring/backup-20260804-0915/` 에 있다.

---

## 4. 남은 약점과 보완안

### 4-1. 🔴 DNS 가 단일 장애점 (미조치)

```
Current DNS Server: 192.168.2.1      ← 공유기 하나뿐. 폴백 없음
```

공유기가 흔들리면 **랜선이 멀쩡해도 이번과 똑같은 증상**이 난다. cloudflared 는 DNS 없이는
못 뜬다. 폴백 리졸버를 추가하면 이 경로가 끊긴다.

```bash
sudo mkdir -p /etc/systemd/resolved.conf.d
sudo tee /etc/systemd/resolved.conf.d/fallback.conf > /dev/null <<'EOF'
[Resolve]
DNS=192.168.2.1
FallbackDNS=1.1.1.1 8.8.8.8
EOF
sudo systemctl restart systemd-resolved
resolvectl status | grep -A2 "Fallback DNS"
```

> 사내 DNS 로만 풀리는 내부 도메인이 있으면 그것부터 확인할 것. 공유기가 우선이고
> 폴백은 그게 죽었을 때만 쓰이므로 일반적으로는 안전하다.

### 4-2. 🔴 외부 감시가 없다 (미조치 — 가장 중요)

이번 22.5시간 장애를 아무도 몰랐던 진짜 이유다.

**끊긴 네트워크 안에 있는 감시 시스템은 자기 네트워크가 끊겼다는 걸 알릴 수 없다.**
이건 고칠 수 있는 버그가 아니라 구조적 한계다. watchdog 도 Uptime Kuma 도 서버 안에 있다.

→ **서버 밖에서 도는 감시가 하나 필요하다.** UptimeRobot·Better Stack 등 무료 요금제로 충분하다.

- `https://job.estsw.co.kr` 을 5분 간격으로
- 알림은 **이메일·휴대폰 푸시** 로 (텔레그램도 결국 인터넷을 타지만, 감시 주체가 밖에 있으면 발송된다)

### 4-3. 원격 접속 수단 (미조치)

장애를 알았어도 **밖에서 서버에 들어갈 방법이 없었다.**

| 경로 | 가능? | 이유 |
|---|---|---|
| SSH `192.168.2.104` | ❌ | 내부망 전용 |
| SSH 공인 도메인 | ❌ | 22번 미노출. Cloudflare 는 HTTP/HTTPS 만 통과 |
| CasaOS UI `casa.estsw.co.kr` | ❌ | **같은 터널에 물려 있어 함께 죽는다** |

단 이번 원인(랜선 분리)은 **어차피 물리적 조치가 필요했으므로** 원격 접속이 있었어도 못 고쳤다.
그래도 공유기 다운·터널 설정 문제 등 원격으로 고칠 수 있는 장애가 더 많으므로 갖춰둘 가치가 있다.

**Tailscale** 을 권한다 — Cloudflare Tunnel 과 독립된 경로라 터널이 죽어도 들어갈 수 있고,
공유기 포트를 열지 않아 공격면이 늘지 않는다.

```bash
curl -fsSL https://tailscale.com/install.sh | sh
sudo tailscale up --ssh
tailscale ip -4        # 100.x.x.x — 어디서든 닿는 주소
```

클라이언트(노트북·휴대폰)에 같은 계정으로 로그인 후 `ssh estsoft@100.x.x.x`.

> **반드시 다른 네트워크에서 접속을 검증할 것.** 사무실 안에서만 확인하면 내부망으로 붙은 건지
> Tailscale 로 붙은 건지 구분되지 않는다.

> 공유기 포트포워딩(22번 개방)은 권하지 않는다. 공인 IP 에 SSH 를 직접 노출하면 자동화된
> 무차별 대입이 곧바로 들어온다.

---

## 5. 우선순위

| 순위 | 항목 | 이유 | 상태 |
|---|---|---|---|
| 1 | **외부 감시 등록** | 이번 장애를 22시간 몰랐던 직접 원인 | ⬜ |
| 2 | **DNS 폴백** | 공유기만 흔들려도 같은 장애 재발 | ⬜ |
| 3 | **Tailscale** | 원격으로 고칠 수 있는 장애에 대비 | ⬜ |
| — | watchdog 업링크 판정 | 무의미한 재시작·오진 제거 | ✅ 2026-08-04 |
| — | 재시작 정책 | 이미 되어 있었음 | ✅ 조치 불필요 |

**1·2번은 서버에서 5분이면 끝난다.** 3번은 계정 생성이 필요하다.

---

## 6. 장애 시 확인 순서 (실전)

```bash
# 1) 물리 계층부터 — 이번 원인이 여기였다
ip link show                          # 랜선: state UP 인지
ping -c 2 192.168.2.1                 # 공유기

# 2) 이름 해석
getent hosts cloudflare.com
resolvectl status | head -20

# 3) 터널
docker logs --tail 50 cloudflared-tunnel
docker ps --filter name=cloudflared    # Up 시간이 짧으면 재시작 반복 중

# 4) 감시 시스템이 이미 판단해둔 것
tail -30 ~/monitoring/events.log
tail -40 ~/monitoring/undelivered_alerts.log    # 못 보낸 알림 = 장애 이력
ls -t ~/monitoring/incidents | head -5          # 최근 인시던트 증거
```

텔레그램에서 `/status`·`/incidents` 로도 같은 내용을 볼 수 있다(인터넷이 살아 있을 때).
