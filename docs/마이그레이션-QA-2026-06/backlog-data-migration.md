# 데이터 마이그레이션 백로그 (Vue → Next)

> 코드가 아닌 **기존 저장 데이터**가 신규 경로/스키마와 어긋날 수 있는 항목 기록.
> 프론트 마이그레이션 검증 중 발견된 데이터 측면 이슈만 모음.

- 알림 targetUrl: 백엔드 알림 테이블에 Vue 구경로(/projectListPage 등)로 저장된 레코드가
  있으면 Next에서 404. 경로 일괄 변환(camelCase→kebab) 또는 프론트 리다이렉트 매핑 필요.
  신규 알림은 정상(현재 코드가 신경로 생성). 기존 데이터만 점검 대상.
