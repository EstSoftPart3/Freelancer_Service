# 마이그레이션 검증 진행상황 (Vue → Next.js)

## 큰 흐름
기능 지도(완료) → 가드 검증(완료) → 기능별 내용 검증(진행 중)
→ affiliation 페이지 생성 → 통합 QA 체크리스트 → 백엔드 백로그 재개

## 단계별 현황

- [x] 0. 전체 기능 매핑 → docs/migration-map.md
      Vue/Next 기능 대응 확인. 진짜 누락은 /affiliation 1건.
- [x] 1. 권한 가드 검증 → docs/check-auth-guards.md
      beforeEach→proxy.ts 대조. P1으로 보인 건 백엔드 dev 설정 이슈로 판명.
- [⏸] 2. 백엔드 인증 경계 → docs/backlog-backend-auth.md (백로그 봉인)
      dev 과잉개방/prod 과잉차단. 마이그레이션과 무관한 별개 트랙. QA 후 재개.
- [x] 3. 로그인·회원가입 내용 검증 → docs/check-login.md
      골격 견고. 4건 발견(1=즉시수정·낮음 / 2=기획확인 / 3=백엔드확인 / 4=상태관리보류).
- [x] 4. 계정찾기 3종 검증 → docs/check-account-recovery.md
      아이디찾기·비번재설정. 토큰 분기(유효/만료/사용됨/위조) 검증 완료.
- [x] 5. 로그인 영역 통합 마무리
      check-login + check-account-recovery 합쳐 ❌/⚠️를
      즉시수정/기획확인/백엔드/상태관리 트랙으로 최종 분류.

      ── 로그인 영역 최종 수정 트랙 (2026-06-26 검증 완료, 판정만 확정) ──
      [프론트 즉시수정·확정]
        - 회원가입 성공판정: Next 성공분기를 body.status==='OK' 검사로 복원 (실패해도 200 반환하므로)
        - 아이디찾기 필드: regDt → userCreatedAtDtm 교정 (userType은 일치)
        - STEP-A 길이검증 복원: userId≥4, name≥2
        - /login·/sign-up 로그인상태 재접근 차단 (proxy.ts, 우선순위 낮음)
      [기획 결정 대기]
        - 기업 회원가입 필수필드 3종: 백엔드가 검증·저장 안 함. 필수 유지(백엔드 저장 추가)냐 필수 해제냐 기획 결정 필요
      [상태관리 일괄정리 트랙]
        - 새로고침 복원 깜빡임 (Zustand persist)
      [프론트 수정·낮음]
        - find-id 결과 ?output= PII URL 노출(R3): 쿼리 대신 상태 전달
      [백로그(백엔드 인증) 연결]
        - 비번재설정 STEP-B 재검증 부재(R1): 백엔드 세션 검증 의존
      [가설·검증중 관찰]
        - "실패해도 HTTP 200 + body.status" 패턴이 다른 API에도 있는지 → 프로젝트·알람 검증 시 확인

      ※ 실제 수정은 9번 통합 체크리스트 단계에서 일괄 진행. 지금은 판정만 확정.
- [x] 6. 프로젝트 지원 플로우 검증 → docs/check-project.md
      분기 최다 영역. vue-spec-project.md 명세 추출 후 대조하는 2단계 방식.

      6번 1단계 완료 — docs/vue-spec-project.md 저장. Next 대조(2단계) 진행 중.
      Vue 성공판정은 HTTP상태 기반(로그인의 body.status와 상반 → 가설 기각, 기능마다 다름).
      Vue 원본 버그 2건 발견(handleReject 미정의, COMPANY_MEMBER 버튼 오결선).

      6번 완료 — docs/check-project.md 저장.
      ★최상위 발견: 프로젝트 지원/합격·불합격/인터뷰 확정 플로우 전체가 Next에 미구현(스텁).
      handleApply는 '준비 중입니다' toast 스텁. 액션 모달군 전체 부재.
      조회·표시·지원취소만 이식됨. → '수정'이 아니라 '마이그레이션 미완성'.
- [x] 7. 알람 서비스 검증 → docs/check-alarm.md
      댓글 알림, 알림 클릭 시 링크 이동 등.

      7번 완료 — docs/check-alarm.md 저장. 스텁 없음, 6개 핸들러 전부 충실 이식.
      필드 불일치 패턴 없음(알람은 깨끗). 프론트 실질 수정 항목 사실상 없음.
      ⚠️유일 리스크: 백엔드에 저장된 notificationTargetUrl이 Vue 구경로(camelCase)면
      Next에서 404 가능 → 백엔드/데이터 이슈라 백로그로.

- [x] 7.5 커버리지 점검(전체 서비스 재확인) → docs/coverage-check.md
      migration-map 전체 펼침 + check-*.md 대조. 미검증/스텁의심 우선순위 정렬.

      전체 커버리지 점검 완료 — docs/coverage-check.md.
      결론: 마이그레이션 대부분 완료(미검증 핸들러도 스텁 아닌 실연결). 스텁/미구현 6건만 국소적.
      - P0 복원/구현: /affiliation 페이지, 프로젝트 지원 플로우
      - 판단 필요 4건(신고·소셜로그인·주소검색·캘린더): Vue 대조로 누락/축소 판별 중
      - 나머지 미검증 영역: 동작 정합성 미확인. 검증 깊이(전수 vs 선별) 4건 결과 후 결정.

- [x] 7.6 커버리지 후속 — 판단 필요 4건 Vue 대조
      판단 필요 4건 Vue 대조 완료 — coverage-check.md 5번 섹션.
      결과: 복원 3건(신고·지하철·캘린더 쓰기) + 의도적 스텁 1건(소셜로그인, Vue도 스텁이라 복원 안 함).
- [ ] 7.7 마이페이지 깊은 검증 (3단 선별 중 깊게볼것)
      미검증 영역 처리방침 확정 — 3단 선별:
       [깊게] 마이페이지(개인/기업 권한분기 위험) → 진행 중
       [중간] 게시판·Q&A·공지 CRUD: 필드명/성공판정만 빠른 스캔 예정
       [손테스트] 메인 등 단순 조회성: 9번 통합 체크리스트에서 직접 확인
      마이페이지 먼저 범위 펼침(mypage-scope.md) 후 개인→기업 순 깊은 검증.

      마이페이지 권한 가드 검증 완료 — check-mypage.md.
      누락 4개(프론트 복원, 그중 ProjectPostPage는 P0 프로젝트 영역과 겹침),
      양쪽 구멍 8개(Vue도 가드 없음 → 백엔드 의존, backlog §5).
      ※8개 운명은 백엔드 인증 트랙(dev permitAll)에 묶임 — 백엔드 잠그면 동시 해결.
      다음: 개인 전용 동작 검증 진행 중.

      마이페이지 개인 동작 검증 완료 — check-mypage.md.
      스텁 없음. 단 이력서 폼이 '축소 재구현'(연결됐으나 내용 깎임) = 제3유형 발견.
      P0: 입력3종 누락(사진·첨부·프로젝트이력), 필드명5종+ 불일치(find-id 패턴 확대·백엔드 확인중), 검증 전무.
      P1: 자동기입·상세모달2종·복사옵션·탈퇴후 clearUser 등 복원.
      P2: 성공판정 방식 Next 내부 제각각(HTTP vs body.status) → 잠재 P1.
      조회성(소속·지원현황·스크랩) 골격은 ✅.
      다음: 이력서 백엔드 계약 확인.

      [x] 이력서 백엔드 계약 확인 완료 (2026-06-26 기준, check-mypage.md §2에 이미 기록돼 있었음).
      핵심 결론: 이력서 등록/수정 = 경로 불일치(404) → Content-Type 불일치(415 위험) →
      필드명/구조 불일치(5종+) 3겹으로 완전 작동 불능. '축소 재구현'이 아니라
      '거의 재구현' 필요로 격상. 우선순위는 프로젝트 지원과 동급 P0.
      상세 → docs/check-mypage.md §2 참조.

      [x] 마이페이지 기업 전용 동작 검증 완료 — docs/check-mypage-company.md 참조.
      핵심 결론: 이력서의 3겹 깨짐(경로→Content-Type→필드명) 패턴은 기업 폼에서 재현 안 됨 —
      프로젝트 공고/소속정보/지원자현황/인원관리 모두 제출 계약 일치. 유일 P0는
      프로젝트 폼 '로드' 단계: GET /projects/forms의 skills가 그룹 객체인데 Next가
      평면 string[]로 가정해 스킬 피커 깨짐 → 백엔드 @NotEmpty로 모든 등록 400 →
      사실상 신규 등록 불가. 부가로 편집 프리필 VO에 주소 필드 2종 누락(영향 적음).
- [ ] 8. /affiliation 페이지 신규 생성  [별도 트랙 — 보류]
      0번에서 찾은 진짜 마이그레이션 누락. Vue 원본 데이터·API 명세로 복원.
      ※ '검증'이 아니라 '신규 생성' 작업 → '검증 우선, 수정은 9번 통합' 원칙과 별개 트랙.
      검증 단계 완료 후 별도 트랙으로 보류.
      (의존성 점검: 9번 체크리스트에 affiliation 전용 항목 없음 → 9번 통합 대조는 8번
      산출물 없이 진행 가능. 단 큰 흐름(line 5)은 8→9 순서를 전제하므로, 9번의 브라우저
      수동 테스트에서 /affiliation 404는 알려진 갭으로 남김.)
- [x] 9. 통합 QA 체크리스트 완료 → docs/qa-migration-checklist.md 참조.
      핵심 결론: 6개 check-*.md 통합 완료. 전체 71개 항목(정상 27·문제 39·미검증/갭 5),
      확정 P0 7개 — 이력서 3겹 깨짐(3건), 기업 프로젝트 공고 등록 불가(skills 구조
      불일치, 1건), 프로젝트 지원확정/합격불합격/인터뷰시간확정 미구현(3건).
      미검증 영역 4개(게시판·Q&A·공지 CRUD, 메인/단순 조회) + 알려진 갭 1개
      (/affiliation 페이지 부재).
- [ ] 9.5 qa-migration-checklist.md 기반 브라우저 수동 테스트  ★현재 여기
      메모: 체크리스트 항목 71개를 실제로 브라우저에서 눌러보며 재현 여부 확인.
      알려진 문제(39개)는 재현만 확인하고 수정하지 않음 — 수정은 이 테스트 이후
      별도 단계에서 일괄 처리. 미검증 영역(게시판·Q&A·공지 등)은 이 테스트에서
      처음 손으로 확인하게 되므로 새로운 발견이 나올 수 있음.
- [ ] 10. [백로그 재개] 백엔드 인증 경계 재설계
      엔드포인트 공개/보호 매트릭스 설계부터.

## 원칙
- 기능당 새 세션. migration-map의 해당 행 경로만 열기. 다른 기능 안 읽기.
- 프론트 마이그레이션 검증만. 백엔드 인증 경계는 백로그(2번)로 분리.
- 각 세션 결과는 docs/check-*.md 로 저장.

## ★P0 미구현/복원 트랙 (신규 구현·복원 필요, 일반 수정과 분리)

[P0 — 핵심 신규구현/복원]
- 프로젝트 지원 플로우 전체(지원·합격불합격·인터뷰) — 신규 구현, 규모 큼
   (ResumeSelectModal / ApplyStatusModal / InterviewSelectModal 등 액션 모달군 전체 미구현)
- /affiliation 공개목록 페이지 — 복원(현재 404). Vue AffiliationListPage 기준

[P1 — 명확한 누락, 복원 (백엔드 API 기존재 → 프론트 위주)]
- 게시판 신고 모달(ReportModal) — POST /report 있음, 프론트만
- 캘린더 일정 쓰기(ScheduleRegisterModal) — POST/PUT/DELETE 있음, 프론트만
- 지하철역 검색(SubwaySearchModal) — ★Kakao Maps SDK 로드/키 설정 선행 필요

[복원 안 함 — 기록만]
- 소셜로그인 — Vue 원본도 '준비 중' 스텁. Next 충실 이관. 누락 아님.

※ 이건 check-*.md의 일반 수정 항목과 별개. 검증 완료 후 구현 우선순위 별도 결정.

## 기획-구현 갭(마이그레이션 범위 외)
- 프로젝트 기업지원 '전체합격/개별합격' 분기: Vue 원본에 미구현(키워드·상태전이 액션 모두 부재).
   마이그레이션 누락 아님. 신규 기능 개발 여부는 별도 기획 결정 필요.
