# docs/

프로젝트 문서와 운영 스크립트. **루트에 있는 것은 계속 참조하는 운영 문서**이고,
하위 폴더는 시기별 작업 기록(아카이브)이다.

> ⚠️ `도구/` 와 아카이브 폴더의 `.py` 는 **DB 비밀번호가 평문**이라 git 에 올리지 않는다.
> 이 폴더에서 git 이 추적하는 것은 아래 5개 `.md` 뿐이다.

---

## 루트 — 운영 문서 (추적됨)

| 파일 | 내용 |
|---|---|
| `배포-가이드.md` | **실서버 배포 절차서.** 혼자 처음부터 끝까지 할 수 있게 쓴 것. 겪은 함정 포함 |
| `서버-인프라-현황.md` | 도메인 → 서비스 매핑, 컨테이너·포트·경로 지도 |
| `원격관리-구축.md` | 장애 대응 현황과 남은 보완안(외부 감시·Tailscale) |
| `seo-deployment-guide.md` | 네이버·구글 검색엔진 등록 절차 |
| `인수인계서.html` / `.pdf` | 마이그레이션 완료 시점 산출물 (2026-07-14) |

**배포하려면 `배포-가이드.md` 부터 읽을 것.**

---

## `도구/` — 계속 쓰는 스크립트

전부 **드라이런이 기본**이고 `--apply` 를 명시해야 실제로 쓴다.

| 스크립트 | 용도 |
|---|---|
| `db-backup.py` | 전체 스키마 덤프 → `C:/dev/db-backup/YYYYMMDD-HHMM/` |
| `db-restore.py` | 덤프 복원 |
| `db-restore-drill.py` | 임시 스키마에 복원해 백업이 실제로 살아나는지 리허설 |
| `cleanup-userdata.py` | **사용자 생성 데이터 정리.** 공통코드·지역코드·계정은 보존 |
| `phase7-accounts.py` | 관리자 `estsw_admin`(148) + 봇 `bot_01~05`(149~153) 생성 |
| `phase8-bot-accounts.py` | 봇 `bot_06~25`(154~173) 증설 + 01~05 닉네임 교체 |

### 🔴 알아둘 것

- **공통코드(`TBL_COMMON_CODE_C`)·지역코드(`TBL_AREA_C`)는 데이터가 아니라 설정값이다.**
  게시판 카테고리·유형·회원 구분이 전부 여기 있어서 지우면 글쓰기·로그인·목록이 깨진다.
  `cleanup-userdata.py` 가 자동으로 보존한다.
- **`phase7-accounts.py` 는 `user_id='admin'` 계정을 템플릿으로 복제한다.**
  회원을 전부 지우면 이 스크립트가 멈춰 계정을 다시 만들 수 없다.
  그래서 `cleanup-userdata.py` 는 기본적으로 관리자·봇·템플릿 계정을 남긴다.

```bash
# 권장 순서
!python "C:/dev/Freelancer_Service/docs/도구/db-backup.py"
!python "C:/dev/Freelancer_Service/docs/도구/cleanup-userdata.py"           # 드라이런
!python "C:/dev/Freelancer_Service/docs/도구/cleanup-userdata.py" --apply
```

---

## 아카이브

### `고도화-2026/` — 플랫폼 고도화 Phase 0~9 (2026-07-28 ~ 08-04)

Epic [#298](https://github.com/EstSoftPart3/Freelancer_Service/issues/298) 완료.
DB 이관부터 실서버 배포까지 9개 Phase.

| 경로 | 내용 |
|---|---|
| `고도화-실행계획.md` | 전체 계획 + 진행 상태 보드 + Phase 별 완료 기록 |
| `인계/` | Phase 간 세션 인계 문서 (phase3·6·8) |
| `확인체크리스트/` | Phase 별 육안검증 절차와 결과 |
| `스크립트/` | 일회성 DDL·데이터 정정 스크립트 (실행 완료됨) |
| `야간작업-컨펌요청.md` | Phase 0 착수 시 컨펌 요청 |

> 스크립트는 **이미 실행이 끝난 것들**이다. 다시 돌리지 말 것.
> 문서 안의 `docs/xxx.py` 경로 표기는 이동 전 기준이라 지금과 다르다(이력 기술이라 그대로 뒀다).

### `커뮤니티-고도화-2026-07/` — 커뮤니티 UI 개편 (2026-07-06 ~ 07-21)

화면기획서, 커뮤니티 세부목록, 베스트글 시드, 로그인 진단 스크립트.

### `마이그레이션-QA-2026-06/` — Vue → Next.js 마이그레이션과 QA (2026-06)

화면별 점검표(`check-*.md`), QA 절차·결과, 마이그레이션 매핑, 백로그.
