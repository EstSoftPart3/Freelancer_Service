# 고도화-2026 시연 대본

> 실서버 `https://job.estsw.co.kr` · **2026-08-25 오전 10:30** · 소요 약 65분

---

# 0. 준비 — ✅ 8/24 완료

데이터 준비는 전날 끝냈다. **당일 아침에 할 일은 없다.** 아래는 상태 요약과 문제 생겼을 때 쓸 명령이다.

## ① 프로젝트 공고 — 적용됨

전부 지원마감(29건)이던 것을 **모집중 6 / 모집예정 1 / 마감 22**로 돌려놨다.

| 공고 | 상태 |
|---|---|
| 81 LG CSMS · 77 신한 슈퍼SOL · 97 금융 백엔드 · 98 쇼핑몰 관리자 · 79 LG CNS AI | 모집중 |
| **105** 신한 알림테스트 | **8/25 마감** ← "종료일 당일에도 채용중" 시연용 |
| 100 | 모집예정 |

## ② 커뮤니티 시드 — 등록됨 (board_sq 203~208)

게시글 4 + Q&A 2 + 댓글 16 + 답변 3, 봇 계정에 자동 분배. 조회수 20~300.

| 화면 | 기간 | 시드 전 | **10:30 시점** |
|---|---|---|---|
| `/community` **추천글** | 주간 7일 | 0건 | **5건** |
| `/community/list` **지금 인기** | 1일 | 0건 | **4건** (08:46 글만 24시간을 넘겨 빠진다) |
| `/community` **베스트글** | 월간 30일 | 4건 | **5건**, 3·5위가 시드 글 |
| `/board` 최신순 | — | 테스트 글 | **시드 4건이 상단**, 카테고리 4종 전부 |

10:30은 다섯 건 중 네 건이 살아 있는 시간대다. **시연이 12:20을 넘기면 지금 인기가 3건, 14:35 이후면 0건**이 되니 늦어지면 주간 탭으로 넘어가면 된다.

## ③ 시연 중 등록할 2차 시드 — 준비됨

**`docs/고도화-2026/시연용-시드-2차.json`** (게시글 3 + Q&A 1 + 답변 2 + 댓글 9). §D6에서 등록·회수를 실제로 보여줄 때 쓴다.
**1차와 제목이 겹치지 않게 따로 만들었고**(같은 걸 다시 넣으면 중복 제외로 0건) 실서버 미리보기로 검증했다 — 경고 0건.
시연 직전에 파일을 열어 전체 복사해 두면 붙여넣기부터 바로 시작할 수 있다.

## ④ 문제 생기면 쓸 명령

```
# 공고 상태 확인 (읽기만)
!powershell -ExecutionPolicy Bypass -File "C:\dev\Freelancer_Service\docs\고도화-2026\스크립트\demo-prep.ps1" -Check

# 공고 날짜 원복 / 시드 회수
!powershell -ExecutionPolicy Bypass -File "C:\dev\Freelancer_Service\docs\고도화-2026\스크립트\demo-prep.ps1" -Rollback
!powershell -ExecutionPolicy Bypass -File "C:\dev\Freelancer_Service\docs\고도화-2026\스크립트\demo-seed.ps1" -Revoke
```

## ⑤ 계정

| 구분 | ID / PW |
|---|---|
| 개인 (주 시연) | `qkdnf325` / `{{TEST_USER_PW}}` |
| 개인 (타인 역할) | `qkdnf3255` / `{{TEST_USER2_PW}}` |
| 기업 | `estsoft` / `{{TEST_CORP_PW}}` |
| 관리자 | `admin` / `{{ADMIN_PW}}` |
| 관리자(Phase 7 신설) | `estsw_admin` / `{{CORP_PW}}` |

**창 3개**: ① FO 일반창(`qkdnf325`) ② FO 시크릿창(`qkdnf3255`, VOC 시연 전용) ③ BO(`admin-job.estsw.co.kr`)
**10:20까지** 세 창 로그인을 끝내 두면 시연 중 로그인 화면을 볼 일이 없다.

## ⑥ 알아둘 것

- `qkdnf325`의 현재 닉네임이 **"닉변"**이다. 화면에 노출되니 §B1에서 그럴듯한 값으로 바꾸면서 시연하면 자연스럽다.
- 게시판 최신순 **5번째**에 `배포 게시글 테스트합니다`(8/4, 조회 1)가 있다. 시드 4건에 밀려 첫 화면 하단이라 눈에 덜 띈다. 지우려면 BO에서 삭제하되 **게시글은 복구 UI가 없다.**
- `[시연] (이번주) …` 제목의 옛 데모글 5건(7월 초, 조회수 부풀림)이 남아 있다. **전체 기간 베스트 1~5위를 독식**하지만 화면에서 전체 기간을 쓰는 위젯이 없어 **시연 중에는 안 보인다.** 게시판 목록 뒤쪽 페이지에만 있다.

---

# A. FO 비로그인 — 10:30~10:40


### A4. 커뮤니티 — 카테고리와 베스트 기준 `Phase 4·3`
- **어디** `/board`
- **한다** 카테고리 칩 클릭 → 커뮤니티 홈 "베스트글" 옆 **ⓘ** → 목록 사이드바에서 **지금 인기 / 주간 / 월간** 탭 전환
- **보인다** 칩 5개(전체·자유·현장정보·기능요청·정보) + 행마다 pill 뱃지

---

# B. FO 개인 로그인 `qkdnf325` — 10:40~11:00

### B1. 닉네임 체계 `Phase 2`
- **어디** `/mypage/edit`
- **한다** 닉네임 연필 아이콘 → 중복 닉네임(퇴근각) 시도 → 정상 닉네임으로 저장
- **보인다** 중복은 토스트 거절 / 저장 후 커뮤니티 작성자명이 바뀜 / **이름(실명) 칸은 readOnly 그대로** / 헤더 우상단은 **실명 유지**(본인만 보는 영역)

### B2. 글쓰기 — 카테고리 + 현장정보 양식 `Phase 4` 🔴
- **어디** `/board` → 등록
- **한다** ① 카테고리 없이 등록 → ② **현장정보** 선택 → ③ **자유**로 변경 → ④ 다시 현장정보 → 양식에 `판교 B동` 입력 → ⑤ **자유**로 변경 → ⑥ 다시 현장정보
- **보인다** ① "카테고리를 선택해주세요" ② ■ 현장명/위치/공정·업무/… **양식 자동 주입** ③ **비워짐** ④ 재주입 ⑤ **내용 보존** ⑥ **확인창**

### B4. 모바일 375px + 길찾기 `Phase A`
- **어디** F12 → 반응형 375px → 프로젝트 상세, 마이페이지 캘린더
- **보인다** 제목·회사·**지원하기 버튼이 최상단**(전엔 자격조건이 먼저) / 캘린더 일정 제목이 **칸 안에**
- **이어서** 데스크톱 복귀 → `/projects` 지도 탭 → 핀 → **경로 찾기** → 카카오 지도에 출발지·목적지 채워짐

### B5. 고객의 소리 `Phase 5` 🔴 신규 기능
- **어디** 창① `/voc` → **창②(시크릿, `qkdnf3255`)**
- **한다**
  1. 창① 등록 → **비공개 체크박스가 기본 ON** (태그 입력란 없는 게 정상)
  2. 제목 `[시연] 비공개 문의`로 등록
  3. 창② `/voc` 목록 확인
  4. 창② 그 행 제목 hover → 주소창에 `/voc/{sq}` 직접 입력
     (sq는 창①에서 그 글을 열면 주소창에 보인다. 기존 비공개 글 **172·173**을 써도 된다)
- **보인다** 창②에서 **목록엔 보이지만** 제목이 회색·클릭 불가 + "작성자와 운영자만 볼 수 있는 문의입니다" / 직접 접근은 토스트 **한 번** 후 목록 복귀(403)
- **한마디** "감추면 문의 0건인 게시판처럼 보여서, 목록엔 남기고 상세만 막는 쪽으로 정했습니다."
- **메일** 등록 즉시 운영자 메일함(`freelancer.service.platform@gmail.com`)에 접수 알림. **본문에 문의 내용은 없다** — 비공개 사본이 외부 메일함에 남지 않게 의도적으로 뺐다. 메일의 "관리자 페이지에서 확인하기" → BO 상세 패널이 **자동으로 열린다**

> **B6(알림 확인)은 §D3에서 답변을 단 뒤** 창①로 돌아와서 한다 →
> 헤더 종 아이콘 → "내 문의에 답변이 등록되었습니다." → 클릭 시 `/voc/{sq}`로 이동

---

# C. FO 기업 로그인 `estsoft` — 11:00~11:05

### C1. 프로젝트 등록 날짜 규칙 `Phase 3`
- **어디** `/mypage/project-post`
- **한다** ① 수행 `9/1~9/3` + 모집 `8/25~10/31` → ② 수행 `9/1~2027-02-28` + 모집 `8/25~11/30`
- **보인다** ① "모집 종료일이 프로젝트 종료일보다 늦습니다" 차단 / ② **정상 등록** (모집종료가 수행시작보다 뒤 = 수행 중 추가 모집)
- **한마디** "수행 중 추가 모집은 막으면 안 됩니다. 실측상 공고의 3분의 2가 이 패턴이었습니다."
- ⚠️ ②로 등록한 공고는 실데이터로 남는다. 제목에 `[시연]`을 붙이고 §F에서 지울 것

### C2. 단가 + 협의 동시 입력 `Phase 6`
- **한다** 단가 `3,000,000` 입력 + "단가 협의" 체크
- **보인다** 둘 다 저장 → 상세에 `단가 : 3,000,000원 / 단가 협의`, 카드에 뱃지 동시 표시

---

# D. BO 관리자 `admin-job.estsw.co.kr` — 11:05~11:35

### D1. 사이드바와 유저 관리 `Phase 6·2`
- **보인다** **소속 관리**·**프로젝트 목록** 신설 / "프로모션 & 이벤트" 그룹 제거(깨진 링크였다) / 유저 목록에 **닉네임 컬럼**, 닉네임·실명 둘 다 검색됨

### D2. 게시물 관리 `Phase 4·5`
- **한다** 카테고리 필터 + 유형 필터 **동시** 적용
- **보인다** 두 필터가 서로를 지우지 않음 / 유형 뱃지 **일반·Q&A·답변** 정상 / **고객의 소리 글도, VOC 답변도 이 목록에 없음**
- **한마디** "답변에 붙이던 가짜 코드 1404가 공통코드 '고객의소리'와 겹쳐서, VOC 글이 '답변'으로 표시될 상태였습니다. 예약 구간으로 옮기고, 조건을 '공지만 빼기'에서 '일반·Q&A만 넣기'로 뒤집었습니다."

### D3. 고객의 소리 관리 `Phase 5`
- **한다** §B5에서 등록한 문의 열기 → **답변 등록** → 처리상태 필터에서 미답변+답변완료 **둘 다** 선택
- **보인다** 상태가 **답변완료**로 전환 / 필터 다중 선택이 풀리지 않음
- **→ 창①로 돌아가 알림 확인**(§B6)

### D4. 소속 관리 `Phase 6`
- **보인다** 22건 + **인증완료/미인증 뱃지**, **소속원 수**(0명은 회색) / **대표자명·사업자번호로도 검색** / **컬럼 헤더 클릭 정렬** / 소속명 클릭 → 수정 다이얼로그
- **한마디** "새 API 없이 기존 유저 관리 API를 재사용했습니다."

### D5. 프로젝트 관리 `Phase 6` 🔴 신규 화면
- **한다**
  1. 모집상태 필터 **모집중** → **모집중 + 마감 동시 선택**
  2. 공고 열어 제목·단가 수정, 단가 + 협의 동시 저장
  3. **모집 종료일을 수행 종료일보다 뒤로** 저장
  4. 삭제 → **"삭제 포함" 스위치** ON → 복구 ↺
  5. FO `/projects`와 대조
- **보인다** 1. 모집중 6건, 다중 선택 유지 / 3. **"모집 종료일이 수행 종료일보다 늦을 수 없습니다"** / 4. 취소선으로 표시 후 복구 / 5. FO에도 즉시 반영
- **한마디** "종료일 당일 자정부터 마감으로 잡히던 버그가 있었습니다 — '오늘까지 모집'인 공고가 아침부터 지원마감이었죠. 오늘 마감인 105번 공고가 지금 `모집중`으로 보이는 게 그 수정입니다."
- **왜 등록 기능이 없나** 공고 등록은 기업 회원의 행위라 BO가 대신 쓰면 작성자 소속이 어긋난다(의도한 설계)

### D6. 커뮤니티 시드 — 등록부터 회수까지 `Phase 8` 🔴 하이라이트
`게시물 관리 > 커뮤니티 시드` · **등록 / 회수 두 탭**으로 되어 있다 · 약 8분

> 붙여넣을 파일은 **`docs/고도화-2026/시연용-시드-2차.json`** (게시글 3 + Q&A 1 + 답변 2 + 댓글 9).
> **어제 넣은 1차와 제목이 겹치지 않게 따로 만들어 뒀다** — 1차를 다시 넣으면 중복 제외로 0건이 된다.
> 시연 전 이 파일을 열어 전체 복사(Ctrl+A, Ctrl+C)해 두면 매끄럽다.

**① 등록 탭 — 프롬프트 (1분)**
- 요청 건수 `4` → **프롬프트 복사** → 메모장에 붙여 보여준다
- **한마디** "이 프롬프트를 외부 AI 무료 채팅에 넣으면 JSON이 나옵니다. 원안은 Claude API 야간 봇이었는데, **운영 비용 0**으로 바꾸려고 사람이 한 번 거치는 방식으로 만들었습니다."

**② 옵션 확인 (30초)**
- 작성일시 분산 **TODAY**, 조회수 **20~300**, 카테고리 균등 배분 체크
- **한마디** "`PAST`는 초기 대량 투입용이고 `TODAY`가 매일 운영용입니다. PAST로 매일 돌리면 새 글이 계속 과거로 흩어져서 목록 상단이 갱신되지 않습니다."

**③ 붙여넣기 → 미리보기 (2분)** ← 이 화면이 핵심
- 2차 JSON 붙여넣고 **미리보기**
- **보인다** AI는 제목·본문·댓글 문구만 줬는데, 표에는 **작성자(봇 계정)·작성일시·카테고리·조회수·댓글 수·채택상태가 채워져 있다**
- **한마디** "AI에게 200건 균등 분배까지 시키면 반드시 어긋납니다. 그래서 **콘텐츠는 AI가, 메타데이터는 서버가** 맡도록 나눴습니다. 추천수만은 넣지 않는데, 추천 테이블 재집계 값이라 값만 채우면 실사용자가 추천을 누르는 순간 무너집니다."

**④ 등록 → FO 확인 (2분)**
- **등록** → 게시글 4 / 답변 2 / 댓글 9 등록 완료 메시지
- **창①(FO)로 전환** → `/community` 또는 `/board` 새로고침 → **방금 글이 최신 상단에 뜬다**
  (SSR 캐시로 안 보이면 **Ctrl+Shift+R** 강력 새로고침)
- **한마디** "여의도 증권사 글이 방금 만든 겁니다."

**⑤ 회수 탭 → 되돌리기 (2분)**
- **회수 탭** → **등록 회차별 회수**에 방금 회차가 기록돼 있다 → **이 회차 회수** → 확인
- **창①(FO) 새로고침** → **사라졌다**
- **한마디** "회수는 댓글 → 답변 → 게시글 순서로 지우고, 봇이 **남의 글에 단 댓글** 때문에 살아남는 글은 댓글 수를 다시 세줍니다. 안 그러면 실사용자 글에 영원히 틀린 댓글 수가 남습니다."
- **한마디** "작성일시를 과거로 흩뿌리기 때문에 **날짜로는 회차를 구분할 수 없습니다.** 그래서 등록할 때 받은 글 번호를 브라우저가 기억해 뒀다가 그것만 정확히 내립니다."

**🔴 절대 누르지 말 것 — 아래쪽 "광역 회수"**
봇 계정이 남긴 **모든 글**이 내려간다. 어제 넣은 1차 시드 6건과 7월에 넣은 봇 글까지 전부 사라져서 §A4에서 보여준 커뮤니티가 비어버린다. 화면만 가리키며 "회차 기록이 없을 때 쓰는 비상용"이라고 설명하고 넘어갈 것.

**⚠️ 등록한 탭에서 회수해야 한다** — 회차 기록은 그 브라우저에 남는다. 중간에 탭을 닫았으면 `demo-seed.ps1 -Revoke`가 아니라 광역 회수밖에 남지 않는다(1차까지 날아간다).

### D7. 권한 강화 `Phase 7`
- **한다** 로그아웃 → `estsw_admin` / `{{CORP_PW}}` 로그인 → 메뉴 훑기
- **보인다** 전 메뉴 정상
- **한마디** "관리자 API 7종에 권한 검사를 걸었습니다. 개인 계정 토큰으로는 403입니다. 봇 25개는 닉네임을 사람처럼 지어서(커밋로그·야근의달인…) 화면에서 봇 티가 안 납니다."

---

# E. 마무리 — 화면에 없지만 말할 것

| Phase | 한 줄 |
|---|---|
| 0 | DB 이관. `@Entity` 17개 중 15개가 **빈 고아 테이블을 자동 생성**하고 있어 JPA를 걷어냈다 |
| 1 | `TBL_BOARD_M`에 **PRIMARY 말고는 인덱스가 하나도 없었다** |
| A | 업로드 **확장자 화이트리스트 신설** — `.jsp`·`.exe`가 그대로 통과하던 상태였다 |
| 2 | 회원가입이 **실패해도 HTTP 200**으로 나가 "가입 완료"로 보이던 결함 |
| 4 | 토큰 없는 글쓰기가 **500 + SQL 노출**이었다. 401이 아니라 500이라 자동 재발급도 안 돌아 토큰 만료 시 글쓰기가 막혔다 |
| 5 | BO 대시보드가 **삭제된 글까지** 보여주던 것, 관리자가 남의 글을 지우면 **0행 반영 후 조용히 성공**하던 것 |
| 9 | 기존 포트를 물려받아 **프록시·DNS 설정 변경 0건**으로 교체. 롤백은 구 컨테이너 재시작 한 줄 |

---

# F. 시연 후 정리

- [ ] **2차 시드가 회수됐는지 확인**(§D6-⑤) — 안 했으면 회수 탭에서 그 회차 회수
- [ ] `[시연]` 붙인 것 삭제 — VOC 1건(§B5), 게시글(§B2), **프로젝트 공고 1건(§C1)**
- [ ] BO에서 삭제한 프로젝트 **복구 확인**(§D5-4)
- [ ] 바꾼 닉네임(§B1)·소속 정보(§D4)·프로젝트 제목/단가(§D5-2) 원복
- [ ] 시드 6건과 공고 날짜는 **그대로 두길 권한다** — 커뮤니티와 메인이 채워져 보인다. 되돌리려면 `demo-seed.ps1 -Revoke` / `demo-prep.ps1 -Rollback`
