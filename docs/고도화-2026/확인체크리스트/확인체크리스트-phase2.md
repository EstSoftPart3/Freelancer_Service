# Phase 2 육안 확인 체크리스트

> 2026-07-29 · 닉네임 전면 치환(PR #306 머지 완료, NOT NULL DDL 적용 완료) 검수용
> 코드·API 검증은 전부 통과한 상태다. 아래는 **브라우저로만 확인 가능한 것**들이다.

## 서버 상태

| | 주소 | 상태 |
|---|---|---|
| 백엔드 | `localhost:8080` | ✅ 기동 중 |
| FO | `localhost:3000` | ✅ 기동 중 |
| BO | `localhost:5173` | ⛔ **미기동** — BO 확인하려면 아래 명령 필요 |

```
cd C:/dev/Freelancer_Service/frontend/react_js && npm run dev
```
BO는 **5173 고정**이다(백엔드 CORS 화이트리스트). 다른 포트로 뜨면 API가 막힌다.

> ⚠️ **BO 쿠키 함정**: BO에 로그인한 브라우저로 FO를 열면 관리자 쿠키 때문에 FO가 400을 뱉는다.
> BO는 **다른 브라우저나 시크릿 창**에서 확인할 것.

## 테스트 계정

- 개인: `qkdnf325` / `{{TEST_USER_PW}}`
- 기업: `estsoft` / `{{TEST_CORP_PW}}`
- 관리자(BO): `admin` / `{{ADMIN_PW}}`

---

## FO 체크 (7건)

- [v] **1. 커뮤니티 목록 — 작성자가 닉네임** ✅ 확인 완료
  `/board`, `/qna`, `/community` 목록의 작성자 자리에 **실명이 아닌 닉네임**(`qkdnf325`, `testuser1` 등)이 보이는지.
  → 모바일 폭(375px)에서는 `BoardCardList`가 렌더되므로 **데스크톱·모바일 둘 다** 볼 것.

- [v] **2. 게시글 상세 + 댓글 작성자** ✅ **dev 서버 재기동 후 해소 — 렌더 검증 완료**
  `/board/91` 200 정상. 본문 작성자 `qkdnf3255`, 댓글 작성자 `admin` 렌더 확인. 실명 0건.
  → 브라우저로도 한 번 봐주면 좋지만, 렌더 결과는 이미 확인된 상태다.

- [v] **3. Q&A 답변 작성자** ✅ **동일하게 해소 — 렌더 검증 완료**
  `/qna/54` 200 정상. 답변 작성자 `testuser1`·`testuser2` 렌더 확인. 실명 0건.
  (첫 답변은 삭제된 글이라 안 보이는 게 정상)

- [v] **4. 헤더** ✅ **요구 변경 반영 완료 — 헤더는 실명으로 되돌림**
  사용자 지시(2026-07-29): "헤더에는 이름으로 표시해".
  헤더는 본인에게만 보이는 개인 영역이라 공개 노출 지점이 아니므로 실명을 쓴다.
  `CommonHeader.tsx` 3곳을 `userNickname` → `userNm`으로 원복.
  → 재기동 후 우상단 드롭다운·모바일 메뉴가 **실명**으로 뜨는지만 한 번 봐주면 된다.

- [v] **5. 회원가입 닉네임 입력** ✅ 확인 완료 — `/sign-up`, `/sign-up?loginType=COMPANY`
  - 닉네임 칸이 보이는지 (개인=이름 아래, 기업=담당자 이름 옆)
  - 입력 멈추고 0.5초 뒤 **자동 중복확인**이 도는지
  - `testuser1` 입력 → "이미 사용 중인 닉네임입니다."
  - `a` 또는 `한 글!` 입력 → 형식 안내 문구
  - 정상 닉네임 → 라벨 옆에 ✓ 체크 아이콘

- [v] **6. 마이페이지 닉네임 변경** ✅ 확인 완료 — `/mypage/edit`
  - 닉네임 행이 보이고 연필 아이콘으로 수정 가능한지
  - 중복 닉네임으로 확정 시 토스트로 거절되는지
  - 정상 변경 후 저장 → 재조회 시 반영, **이름(실명) 칸은 그대로**인지
  - 개인 회원은 실명이 `readOnly`인 정책 유지 확인

- [v] **7. SEO 실명 미노출** ✅ **검증 완료** ← 이번 Phase의 핵심 목표
  `/board/91` SSR HTML(45,888자)에 실명 **0건**. `"@type":"Person"`의 `name` = `qkdnf3255`(닉네임).
  → 브라우저에서 **페이지 소스 보기(Ctrl+U)**로 직접 보실 수 있다.
  ※ 주의: **개발자도구 Elements**로 보면 로그인 상태에서 헤더에 실명이 보인다 — 4번 요구대로 헤더는
  실명이고, 이건 클라이언트에서 그려지므로 **검색엔진이 읽는 SSR 소스에는 없다.** 정상이다.

---

## BO 체크 (4건) — ⏸ **미확인: BO 서버(5173) 미기동**

> 사용자 확인 불가 사유: BO 서버가 안 켜져 있음. 아래 명령으로 띄운 뒤 확인.
> ```
> cd C:/dev/Freelancer_Service/frontend/react_js && npm run dev
> ```

- [v] **8. 유저 목록 닉네임 컬럼** ✅ 확인 완료 (닉네임 컬럼 정상 표시)

- [ ] **8-1. 🐞 유저 목록 컬럼명 누락 — 수정 완료, 재확인 필요**
  사용자 지적: "닉네임과 생년월일을 제외하고 다른 컬럼명이 보이지 않음".
  → **Phase 2 이전부터 있던 기존 버그다.** 헤더가 `column.columnDef.meta?.title`을 읽는데
  `meta.title`이 정의된 컬럼이 **생년월일 하나뿐**이었다(내가 추가한 닉네임이 두 번째).
  나머지 14개 컬럼은 `undefined`가 넘어가 헤더가 빈칸이었고, 닉네임에 제목이 생기면서 대비로 드러났다.
  → 14개 컬럼 전부에 `meta.title` 추가: 회원유형·프로필·아이디·이름·기업명·이메일·휴대폰·성별·
  가입일·수정일·가입경로·활성·삭제·약관동의

> ✅ **아래 3건(9·10·11)은 사용자 확인 결과 문제 없음 — 2026-07-29 종결.**

- [v] **9. 닉네임 검색**
  검색창에 `testuser1`(닉네임) 입력 → 해당 회원이 잡히는지. 실명 검색도 계속 동작해야 한다.

- [v] **10. 유저 수정 드로어**
  수정 열기 → 이름 아래 **닉네임 입력칸**이 있고 기존 값이 채워져 있는지.
  중복 닉네임으로 저장 시 거절되는지.

- [v] **11. 게시판 목록·상세** — **요구 반영으로 변경됨, 재확인 필요**
  - 목록의 작성자 컬럼 = **아이디**(사용자 요구로 닉네임 → 아이디 변경). 정렬도 동작해야 한다
  - 상세 드로어를 열면 작성자 = **실명**(관리 목적), 댓글·답변 작성자 = **닉네임**
  - ※ 댓글/답변이 **빈칸으로 보이면 회귀**다 — 백엔드 필드명 변경이 BO까지 파급된 부분이라 이 항목이 중요하다

- [ ] **12. 🐞 공지사항 관리 — 500 수정 완료, 재확인 필요**
  사용자 지적: 공지사항 관리 진입 시 500.
  → **Phase 2가 만든 회귀였다.** `AdminNoticeMapper`가 커뮤니티 `BoardListDTO`를 재사용하는데
  별칭이 `userNm`이라, Phase 2의 `userNm` → `userNickname` rename 후 값이 조용히 null로 빠졌고
  BO zod가 `userNm: z.string()`을 요구해 터졌다.
  → resultType을 admin 전용 `AdminBoardListDTO`로 교체. API 응답에 `userNm='최고관리자'` 복구 확인.

---

## 확인 후

문제 없으면 다음 세션에서 **Phase 3** 착수.
Phase 3에는 Phase 2에서 발견한 **프로젝트 검색 버그(3-C)**가 이관돼 있다 — FO가 보내는 `'회사명'`에
대응하는 백엔드 분기가 없어 조용히 전체검색으로 폴백되고 회사명은 검색되지 않는 문제.
자세한 내용은 `docs/고도화-실행계획.md`의 `### 3-C` 참조.
