# Phase 5 피드백 반영 — 재검증 체크리스트

> ## ✅ 2026-07-31 종결 — 수정한 그대로 확정
> R-1~R-4 전부 확인 완료. **Phase 5는 육안검증까지 끝났고 PR #316 머지 가능 상태**입니다.
> 아래는 무엇을 어떻게 고쳤는지의 기록입니다.
>
> 커밋 `721ddc4b` (브랜치 `feat/phase-6-7-bo-admin`) · 백엔드 컴파일 + FO tsc + BO 빌드 통과
> 정상이라고 하신 5건(V-2·V-3·V-5·V-6·V-8)은 건드리지 않았습니다.
>
> **확정된 정책**: 비공개 글은 목록에 노출하고 상세만 막는다(제목은 공개). 제목 마스킹은 하지 않는다.

## 준비

```
백엔드  cd backend && ./gradlew bootRun          (8080)
FO      cd frontend/nextjs && npm run dev:clean  (3000)
BO      cd frontend/react_js && npm run dev      (5173)
```

**⚠️ 시드 상태가 바뀌었습니다** (검증 중 확인):

| sq | 제목 | 공개여부 | 답변 | 비고 |
|---|---|---|---|---|
| 172 | 직접 테스트합니다. | 🔒 **비공개** | 1건 | 직접 만드신 글 — **R-1 검증은 이걸로** |
| 173 | [V-9 확인용] 메일 바로가기 테스트 | 🔒 비공개 | 0건 | **R-4용으로 제가 만들었습니다** |
| 167 | [검증] 비공개 문의 테스트 | **공개로 바뀜** | 1건 | V-4 수정 테스트 중 공개로 전환된 듯합니다 |
| 168 | [검증] 공개 문의 테스트 | 공개 | 0건 | |

작성자는 `qkdnf325`, 타인 시점은 `qkdnf3255`입니다.

---

## R-1. 🔴 비공개 글 — 목록엔 보이고 상세는 막힘 (V-1 정책 변경)

말씀하신 대로 **목록 노출 + 상세 진입 차단**으로 바꿨습니다.

`qkdnf3255`(타인)로 로그인 → `/voc`
- **비공개 글(172·173)이 목록에 보이는지** — 이제 보여야 정상입니다
- 그 행에 🔒 **비공개** 뱃지가 붙고, **제목이 회색이며 클릭되지 않는지**
  (마우스를 올리면 "작성자와 운영자만 볼 수 있는 문의입니다" 툴팁)
- 공개 글(167·168)은 평소처럼 클릭되는지
- 주소창에 `/voc/172`를 **직접 입력**하면 → 안내 후 목록으로 돌아오는지 (토스트 **한 번만**)

> API로 확인했습니다: 타인 목록 3건 전부 노출 / `/voc/172` 403 / `/voc/167·168` 200 /
> 작성자 본인은 `/voc/172` 200.

### 짚어둘 점
제목은 이제 **누구에게나 보입니다.** 문의 제목에 "○○회사 급여 미지급" 같은 내용이 들어가면
그건 노출됩니다. 원하셨던 동작이지만, 필요하면 **제목만 "비공개 문의입니다"로 가리는** 방식으로
바꿀 수 있습니다(목록에 줄은 남고 건수도 보입니다). 말씀만 주세요.

## R-2. 🔴 고객의 소리 상세 진입 (V-4 버그 수정)

**원인**: 목록이 만드는 링크가 `/voc/{sq}`가 아니라 `/board/{sq}`였습니다.
서버가 게시판 유형을 `1402면 qna, 나머지 전부 board`로 계산하고 있어서 고객의 소리가 `board`로
떨어졌고, FO는 그 경로로 이동해 400을 받았습니다("게시글이 존재하지 않습니다").

- `/voc` 목록에서 **내 글 제목을 클릭** → `/voc/{sq}` 상세로 들어가지는지
- 알림이 아닌 **목록 경로로** 수정까지 되는지 (V-4에서 못 하셨던 경로)
- 오류 토스트가 **두 번 뜨던 것**도 고쳤습니다 — 잘못된 sq로 들어가 봐도 한 번만 떠야 합니다

> 같은 원인이 알림 링크에도 있었는데 그건 앞서 고쳤습니다. 이번엔 목록 링크였습니다.

## R-3. 🔴 BO 게시물 관리에서 고객의 소리 "답변" 제외 (V-7)

**BO → 게시물 관리**
- **고객의 소리에 달린 답변이 목록에서 사라졌는지**
- 일반·Q&A 답변은 그대로 보이는지 (유형 뱃지 "답변")
- 유형 필터 "답변"이 정상 동작하는지

> 게시글은 걸렀는데 답변은 원글의 유형을 보지 않아 새고 있었습니다.
> API로 확인했습니다: VOC 답변 2건이 실제로 존재하지만 BO 목록의 답변 7건에는 없습니다.

## R-4. 접수 메일 링크 → 상세 패널까지 열림 (V-9)

**`freelancer.service.platform@gmail.com` 받은편지함**에 방금 보낸 메일이 있습니다:
제목 `[고객의 소리] 새 문의가 접수되었습니다 (비공개)`

- 본문의 **"관리자 페이지에서 확인하기"** 클릭
- → BO 고객의 소리 목록이 열리면서 **173번 문의의 상세 패널이 자동으로 열리는지**
- 패널을 **닫으면 그대로 닫히는지** (URL에 파라미터가 남아 다시 열리면 안 됩니다)

> 링크가 `/contents/voc?view=173`으로 바뀌었습니다. BO가 로그인 상태여야 열립니다.

---

## 제가 확인한 것

| 항목 | 결과 |
|---|---|
| 타인 목록에 비공개 글 노출 | 3건 전부 보임 (정책 전환 반영) |
| 타인 → 비공개 상세 | 403 |
| 타인 → 공개 상세 | 200 |
| 작성자 본인 → 자기 비공개 상세 | 200 |
| 목록 응답 `boardType` | `voc` (전에는 `board`였음 = R-2 원인) |
| VOC 답변의 BO 목록 유입 | 답변 2건 존재하나 BO 목록엔 0건 |
| 접수 메일 | 실제 발송 성공, 링크 `?view=173` |
| 빌드 | 백엔드 컴파일 · FO `tsc` · BO `vite build` |

## 정리해두면 좋을 것

검증이 끝나면 시드를 정리하시면 됩니다. 남겨두셔도 동작에는 지장 없습니다.

- 167·168 — Phase 5 최초 검증용
- 172 — 직접 만드신 글
- 173 — R-4(메일 링크)용, 확인 후 삭제 가능
