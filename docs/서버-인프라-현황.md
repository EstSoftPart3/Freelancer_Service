# 서버 인프라 현황

> 조사 2026-08-03 (Phase 9 착수 시점) · 조사 방법: SSH 직접 접속 + `docker inspect` + 외부 HTTP 요청
> **확인된 사실과 추정을 구분해 적었다.** 추정 항목은 🔸로 표시했고 확인 명령을 함께 남겼다.

---

## 1. 서버 기본 정보

| 항목 | 값 |
|---|---|
| 접속 | `ssh estsoft@192.168.2.104` (**내부망 전용** — 외부에서 22번 포트 닫혀 있음) |
| OS | Ubuntu 22.04.5 LTS (x86_64) |
| 메모리 | 15Gi (사용 2.9Gi / 여유 12Gi) |
| 디스크 | `/dev/sda2` 916G 중 **737G 여유** (16% 사용) |
| Docker | 29.1.4 / Compose v5.0.1 |
| 계정 권한 | `estsoft` 는 **docker 그룹 소속**(sudo 없이 도커 조작 가능). `sudo` 는 비밀번호 필요 |
| 관리 도구 | CasaOS (자체 서비스 다수가 127.0.0.1 에서 대기) |

`/DATA/freelancer_project/` 의 소유자는 **root** 다(`uploads/` 만 estsoft). 배포 시 파일 교체에 `sudo` 가 필요하다.

---

## 2. 도메인 → 서비스 매핑

리버스 프록시는 **Nginx Proxy Manager**(컨테이너 `nginxproxymanager`)이고, 외부 노출은 **Cloudflare Tunnel**(`cloudflared-tunnel`)을 통한다.
NPM 관리 UI: `http://192.168.2.104:81`

| 도메인 | 서비스 | 포트 | 근거 |
|---|---|---|---|
| `job.estsw.co.kr` | **구 Vue FO** (교체 대상) | 8082 | HTML 본문에 `vue_js doesn't work properly`·`chunk-vendors` 확인 |
| `job.estsw.co.kr/api/*` | Spring 백엔드 | 8081 | NPM 설정에 `proxy_pass http://192.168.2.104:8081` |
| `admin-job.estsw.co.kr` | **React BO** | 8083 | Vite 산출물 형태(`/images/favicon.svg`) 확인 |
| `www.estsw.co.kr` | 🔸 `homepage`(tomcat) | 8080 | 이름·포트로 추정 |
| `cloud.estsw.co.kr` | 🔸 Nextcloud | 11000 | 이름으로 추정 |
| `office.estsw.co.kr` | 🔸 OnlyOffice | 7400 | 이름으로 추정 |
| `log.estsw.co.kr` | 🔸 Dozzle (도커 로그 뷰어) | 8888 | 이름으로 추정 |
| `status.estsw.co.kr` | 🔸 Uptime Kuma | 3001 | 이름으로 추정 |
| `casa.estsw.co.kr` | 🔸 CasaOS 관리 UI | — | 이름으로 추정 |

🔸 확정하려면:

```bash
docker exec nginxproxymanager sh -c 'grep -A3 -h "server_name" /data/nginx/proxy_host/*.conf' | grep -E "server_name|set \$server|set \$port|proxy_pass"
```

---

## 3. 컨테이너 목록

> **2026-08-04 갱신** — Next.js 배포로 구 3개가 신 3개로 교체됐다(구 컨테이너는 삭제).

### 이 프로젝트 (현행)

| 컨테이너 | compose 프로젝트 | 이미지 | 포트 | 비고 |
|---|---|---|---|---|
| `freelancer-api` | `freelancer-next` | `eclipse-temurin:21-jdk` | 8081→8081 | `~/freelancer/backend` → `/app` (jar 볼륨 마운트)<br>`/DATA/freelancer_project/uploads` → `/app/uploads` |
| `freelancer-next` | `freelancer-next` | `freelancer/frontend-next` | 8082→3000 | **유일하게 이미지를 굽는다**(Next.js SSR) |
| `freelancer-bo` | `freelancer-next` | `nginx:stable-alpine` | 8083→80 | `~/freelancer/admin` → `/usr/share/nginx/html` |
| `mariadb` | `mariadb` | `linuxserver/mariadb:11.4.8` | 3306 | — |
| `redis` | `redis` | `redis:latest` | 6379 (내부 전용) | — |

compose 정의는 `~/freelancer/deploy/docker-compose.yml`(estsoft 소유, sudo 불필요).

### 구 컨테이너 (2026-08-04 제거)

`freelancer-api-server`(8081) · `freelancer-web-server`(8082, Vue) · `freelancer-admin-server`(8083).
CasaOS 앱 `tender_hazel`(UI 제목 `freelancer-api-total`) 소속이었다.
정지 상태로 남아 있어 UI 에서 **새 배포가 죽은 것처럼 오해를 샀고**, 타일 정리 시 함께 삭제했다.
마운트가 전부 `/DATA/freelancer_project` bind 라 삭제로 사라진 데이터는 없다.
정의는 `~/freelancer/rollback/` 에 보관 — 재생성 절차는 `배포-가이드.md` 3장.

**설계 특징** — 백엔드·BO 는 여전히 이미지를 굽지 않는다. 베이스 이미지에 **파일을 볼륨으로 꽂는 방식**이라
배포가 곧 파일 교체다. FO(Next.js)만 SSR 이라 이미지를 굽는다.

`mariadb` 가 이 서버에 있다 → **`db.estsw.co.kr` 이 곧 이 장비**다. DB 백업을 원격이 아니라 로컬에서 뜰 수 있다.

### 인프라·기타

| 컨테이너 | 이미지 | 포트 | 역할 |
|---|---|---|---|
| `nginxproxymanager` | `jc21/nginx-proxy-manager:2.13.5` | 80, 81, 443 | 리버스 프록시 (UI = 81) |
| `cloudflared-tunnel` | `cloudflare/cloudflared:latest` | — | Cloudflare Tunnel |
| `homepage` | `tomcat:9.0-jdk8` | 8080 | 회사 홈페이지 |
| `nextcloud` | `linuxserver/nextcloud:32.0.3` | 11000→80 | 파일 공유 |
| `onlyoffice` | `onlyoffice/documentserver:9.1.0` | 7400→80 | 문서 편집 |
| `dozzle` | `amir20/dozzle:latest` | 8888→8080 | 도커 로그 웹 뷰어 |
| `uptime-kuma` | `louislam/uptime-kuma:1` | 3001 | 가동 감시 |
| `incident-web` | `nginx:alpine` | 8090→80 | 🔸 용도 미확인 |

### 포트 점유 요약 (신규 배포 시 피할 것)

```
80, 81, 443  nginxproxymanager
3001         uptime-kuma      3306   mariadb
7400         onlyoffice       8080   homepage
8081         freelancer-api   8082   freelancer-next
8083         freelancer-bo    8090   incident-web
8888         dozzle           11000  nextcloud
22           sshd             445,139 smbd
9000         casaos-gateway
```

→ **신규 스택은 구 포트(8081·8082·8083)를 그대로 물려받았다.** NPM·Cloudflare 설정 변경 0건.

CasaOS 자체 서비스는 `127.0.0.1` 의 **임의 포트**에 뜬다(재부팅마다 바뀐다).
앱 관리 API 포트를 찾는 법은 `배포-가이드.md` 5장.

### 네트워크

```
bridge  casaos-net  estsoft  freelancer-admin  mariadb_default  monitoring_default
```

현행 3개 컨테이너는 **`estsoft` 한 곳**에만 붙어 있다(`redis`·`mariadb`·NPM 이 전부 여기 있다).
FO 는 `http://freelancer-api:8081/api` 로 백엔드에 닿는다 — `API_INTERNAL_BASE_URL`.
`freelancer-admin` 네트워크는 구 스택이 쓰던 것으로 지금은 사용처가 없다.

---

## 4. 파일 배치

```
/DATA/freelancer_project/          (root 소유)
├── frontend/                      구 Vue 정적 파일 (2026-04-20)
│   ├── index.html, js/, css/, img/, vendor/, master/, php/, ajax/, video/
│   └── default.conf               ← 컨테이너 nginx 설정
├── admin/                         React BO 정적 파일 (2026-03-31)
│   ├── index.html, assets/, images/
│   └── default.conf
├── backend/
│   └── backend-0.0.1-SNAPSHOT.jar (2026-05-04, 88MB)
├── uploads/                       ★ 세 컨테이너가 공유 (5.4MB, estsoft 소유)
└── monitor.log

/var/lib/casaos/apps/<앱>/docker-compose.yml    ← CasaOS 앱 정의 (파일은 root:600 이지만
                                                  디렉터리가 777 이라 estsoft 가 교체는 가능)
    └ tender_hazel/  ← 구 배포 정의. 2026-08-04 ~/freelancer/rollback/ 으로 이관(빈 디렉터리만 남음)

/home/estsoft/freelancer/          ★ 현행 배포 (estsoft 소유, sudo 불필요)
├── deploy/{docker-compose.yml, bo-nginx.conf, .env}
├── backend/backend-0.0.1-SNAPSHOT.jar
├── admin/                         BO dist
├── frontend/nextjs/               FO 소스
└── rollback/                      구 스택 정의 백업(compose·inspect)

/home/estsoft/monitoring/          감시·자동복구·텔레그램 제어 (2026-07-28 구축)
├── watchdog.sh                    1분마다 — 프로브·원인판정·안전재시작
├── control.sh                     1분마다 — 텔레그램 /status·/incidents·/pause·/resume
├── report.sh                      10분마다 — 대시보드 HTML
├── lib.sh                         공통 라이브러리
├── config.env                     설정(0600, 텔레그램 토큰 포함)
├── events.log                     타임스탬프 이벤트 로그
├── undelivered_alerts.log         ★ 발송 실패한 알림 = 네트워크 장애 이력
├── incidents/                     인시던트별 증거 디렉터리
└── state/                         쿨다운·서킷브레이커 상태
```

**감시 시스템이 이미 잘 구축돼 있다** — 새로 만들지 말 것. 상세는 `docs/원격관리-구축.md`.

---

## 5. 🔴 라이브 버전 불일치 — 배포 전 반드시 알아야 할 것

| 대상 | 라이브 빌드 | 상태 |
|---|---|---|
| 백엔드 jar | **2026-05-04** | Phase 0~8 백엔드 변경이 **하나도 반영되지 않음** |
| BO (admin) | **2026-03-31** | 소속·프로젝트 관리, 커뮤니티 시드, 권한 강화 전부 없음 |
| FO (Vue) | 2026-04-20 | Next.js 로 교체 대상 |
| **DB** | **최신** | Phase 1~4 스키마 반영 완료(닉네임 NOT NULL·카테고리 필수·VOC 컬럼) |

**즉 현재 라이브는 "구버전 백엔드 + 신 스키마 DB" 조합이다.**
새 컬럼을 요구하는 경로(회원가입의 닉네임, 게시글의 카테고리 등)는 이미 실패하고 있을 가능성이 높다.

이것이 배포 계획에 주는 영향:

- **"구버전으로 롤백"이 안전지대가 아니다.** 롤백해도 DB 는 그대로라 같은 불일치로 돌아간다.
  롤백은 "정상 상태 복귀"가 아니라 "새 문제 회피"일 뿐이다.
- 그러므로 **백엔드를 먼저 최신으로 올리는 것**이 오히려 위험을 줄인다.
- 배포 전에 라이브에서 회원가입·글쓰기가 실제로 되는지 확인해두면, 배포 후 문제가 새로 생긴 것인지
  원래 깨져 있던 것인지 구분할 수 있다.

---

## 6. 외부 접근 제약 · 장애 이력

- **외부에서 SSH 불가** — `estsw.co.kr`·`casa.estsw.co.kr`·`job.estsw.co.kr` 전부 22번 포트가 닫혀 있다.
  Cloudflare 프록시는 HTTP/HTTPS 만 통과시킨다.
- **모든 웹 서비스가 Cloudflare Tunnel 하나에 물려 있다.** 터널이 끊기면 전 도메인이 동시에 죽고,
  `casa.estsw.co.kr`(CasaOS UI)도 함께 죽어 **원격 복구 수단이 사라진다.**
- **DNS 서버가 공유기(`192.168.2.1`) 하나뿐이고 폴백이 없다** — 단일 장애점.

### 🔴 2026-08-03 장애 (22.5시간) — 원인 확정

**서버 PC 의 랜선이 물리적으로 빠져 있었다.** 랜선 분리 → 게이트웨이 도달 불가 → DNS 실패 →
cloudflared 가 `argotunnel.com` SRV 조회에 실패해 자체 종료 → 재시작 반복 → 전 도메인 530(1033).

- 구간: 08-03 10:39 ~ 08-04 09:08. 서버 재부팅 없음(uptime 175일 유지)
- **복구는 자동이었다** — 케이블 재연결 후 09:08:38 에 터널이 스스로 4개 연결 등록
- 재시작 정책은 **원래 전부 설정돼 있었다**(`always`/`unless-stopped`, 13개 전부)
- 텔레그램 봇도 죽지 않았다 — 인터넷이 끊겨 발송만 실패, 알림 84건이 `undelivered_alerts.log` 에 적재
- **아무도 22시간 동안 몰랐던 이유**: 감시 시스템이 전부 서버 안에 있다.
  끊긴 네트워크 안의 감시는 자기 네트워크가 끊겼다는 걸 알릴 수 없다 → **외부 감시 필요**

**고친 것**: watchdog 이 회선 장애를 터널 장애로 오판해 85회 무의미하게 재시작했다.
`uplink_ok()` 를 추가해 터널/NPM 판정 **전에** 게이트웨이·공인망·DNS 를 먼저 확인하게 했다(08-04 적용).

→ 상세와 남은 보완안(외부 감시·DNS 폴백·Tailscale)은 **`docs/원격관리-구축.md`** 참조.

---

## 7. 자주 쓸 조사 명령

```bash
# 전체 컨테이너 상태
docker ps -a --format 'table {{.Names}}\t{{.Image}}\t{{.Status}}\t{{.Ports}}'

# 특정 컨테이너 볼륨/네트워크
docker inspect freelancer-api-server --format '{{range .Mounts}}{{.Source}} => {{.Destination}}{{println}}{{end}}'
docker inspect freelancer-api-server --format '{{range $k,$v := .NetworkSettings.Networks}}{{$k}} {{end}}'

# 백엔드 로그 (마지막 100줄)
docker logs --tail 100 freelancer-api-server

# 헬스체크
curl -s http://localhost:8081/api/actuator/health

# NPM 프록시 설정 전체
docker exec nginxproxymanager sh -c 'cat /data/nginx/proxy_host/*.conf'

# 터널 상태
docker logs --tail 50 cloudflared-tunnel

# 감시 시스템이 이미 판단해둔 것 (장애 조사 시 여기부터)
tail -30 ~/monitoring/events.log
tail -40 ~/monitoring/undelivered_alerts.log   # 못 보낸 알림 = 네트워크 장애 이력
ls -t ~/monitoring/incidents | head -5

# 업링크 확인 (장애 시 물리 계층부터)
ip link show ; ping -c 2 192.168.2.1 ; getent hosts cloudflare.com
```
