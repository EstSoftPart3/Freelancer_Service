<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="com.example.demo.domain.map.mapper.MapAddressMapper">

	<select id="findCoordinates" resultType="com.example.demo.domain.map.dto.response.AreaCoordinateResponse">
	SELECT latitude, longitude 
	FROM TBL_ADDRESS_S
    WHERE address_sq = #{addressSq} 
	
	</select>
	
</mapper>