<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.domain.admin.mapper.AdminPointPolicyMapper">

    <select id="selectPointPolicy"
            resultType="com.example.demo.domain.admin.dto.AdminPointPolicyResponse">
        SELECT
            point_policy_sq AS pointPolicySq,
            attendance_point AS attendancePoint,
            streak_point AS streakPoint,
            event_point AS eventPoint,
            auto_payment_yn AS autoPaymentYn,
            duplicate_block_yn AS duplicateBlockYn,
            manual_adjust_yn AS manualAdjustYn,
            DATE_FORMAT(reg_dt, '%Y-%m-%d %H:%i:%s') AS regDt,
            DATE_FORMAT(mod_dt, '%Y-%m-%d %H:%i:%s') AS modDt
        FROM TBL_POINT_POLICY_M
        ORDER BY point_policy_sq DESC
        LIMIT 1
    </select>

</mapper>