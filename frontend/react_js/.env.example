# 백엔드 API 베이스 URL (빌드 타임 인라인 — 바꾸면 재빌드 필요)
# 로컬 개발은 비워두면 http://localhost:8080/api 로 폴백한다.
# 운영 배포는 deploy/.env 의 BO_API_BASE_URL 로 주입한다(기본 '/api').
VITE_API_BASE_URL=

# Clerk 잔재 — 현재 코드에서 사용하지 않는다.
VITE_CLERK_PUBLISHABLE_KEY=
