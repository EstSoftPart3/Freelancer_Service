# 🗺️ 지도 기능 상세 분석 문서

## 📋 목차
1. [전체 아키텍처 개요](#전체-아키텍처-개요)
2. [백엔드 구조 분석](#백엔드-구조-분석)
3. [프론트엔드 구조 분석](#프론트엔드-구조-분석)
4. [데이터 흐름 분석](#데이터-흐름-분석)
5. [핵심 기능별 상세 설명](#핵심-기능별-상세-설명)
6. [API 엔드포인트 분석](#api-엔드포인트-분석)
7. [데이터베이스 쿼리 분석](#데이터베이스-쿼리-분석)
8. [지도 표시 원리](#지도-표시-원리)
9. [마커 위치 계산 알고리즘](#마커-위치-계산-알고리즘)
10. [성능 최적화 전략](#성능-최적화-전략)

---

## 🏗️ 전체 아키텍처 개요

### 시스템 구조도
```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   프론트엔드     │    │     백엔드       │    │    데이터베이스   │
│   (Vue.js)      │    │   (Spring Boot)  │    │     (MySQL)     │
├─────────────────┤    ├─────────────────┤    ├─────────────────┤
│ MapSearchPage   │◄──►│ MapSearchController│◄──►│ TBL_PROJECT_M   │
│ - 지도 표시      │    │ - API 엔드포인트  │    │ TBL_ADDRESS_S   │
│ - 마커 표시      │    │ - 비즈니스 로직  │    │ TBL_USER_M      │
│ - 필터링        │    │ - 데이터 변환    │    │ TBL_COMPANY_S   │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│ 네이버 지도 API  │    │ 네이버 Static   │    │ 하버사인 공식    │
│ - 길찾기        │    │ Map API         │    │ - 거리 계산     │
│ - 경로 안내     │    │ - 지도 이미지    │    │ - 반경 검색     │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

---

## 🔧 백엔드 구조 분석

### 1. 컨트롤러 계층 (Controller Layer)

#### 📁 `MapSearchController.java`
**역할**: HTTP 요청을 받아서 적절한 서비스 메서드를 호출하고 응답을 반환

**주요 메서드**:
```java
@GetMapping("/user-address")     // 사용자 주소 조회
@GetMapping("/search")           // 프로젝트 검색
@GetMapping("/naver-route")      // 네이버 길찾기 URL 생성
@GetMapping("/naver/static")     // 네이버 Static Map API 호출
```

**핵심 흐름**:
1. **사용자 주소 조회** (`/user-address`)
   - 사용자 ID로 등록된 주소 정보 조회
   - 위도, 경도, 주소 반환

2. **프로젝트 검색** (`/search`)
   - 사용자 ID 또는 직접 좌표로 검색
   - 반경, 직무, 키워드 필터링
   - 페이징 처리

3. **네이버 Static Map** (`/naver/static`)
   - 네이버 지도 API 호출
   - 실패 시 fallback JSON 응답

### 2. 서비스 계층 (Service Layer)

#### 📁 `MapSearchService.java`
**역할**: 비즈니스 로직 처리 및 데이터 변환

**핵심 메서드**:
```java
getUserLocation(Long userId)                    // 사용자 위치 조회
searchProjectsByUserId(...)                     // 사용자 ID로 검색
searchProjects(MapSearchRequest request)        // 직접 좌표로 검색
convertToResponse(...)                          // DTO → Response 변환
```

**데이터 변환 흐름**:
```
MapProjectDto (DB) → MapProjectResponse (API) → 프론트엔드
```

### 3. 매퍼 계층 (Mapper Layer)

#### 📁 `MapSearchMapper.java` (인터페이스)
**역할**: MyBatis 매퍼 인터페이스 정의

**주요 메서드**:
```java
findUserAddress(Long userId)                    // 사용자 주소 조회
findProjectsWithinRadius(...)                  // 반경 내 프로젝트 검색
countProjectsWithinRadius(...)                 // 검색 결과 개수
```

#### 📁 `MapSearchMapper.xml` (SQL)
**역할**: 실제 SQL 쿼리 실행

**핵심 쿼리**:
1. **사용자 주소 조회**
   ```sql
   SELECT latitude, longitude, address, detail_address, sigungu
   FROM TBL_USER_M u
   INNER JOIN TBL_ADDRESS_S a ON u.address_sq = a.address_sq
   WHERE u.user_sq = #{userId}
   ```

2. **반경 내 프로젝트 검색** (하버사인 공식 사용)
   ```sql
   SELECT 프로젝트정보, 거리계산결과
   FROM TBL_PROJECT_M p
   INNER JOIN TBL_ADDRESS_S a ON p.address_sq = a.address_sq
   WHERE (6371 * acos(cos(radians(#{userLat})) * cos(radians(a.latitude)) * ...)) <= #{radius}
   ORDER BY 거리 ASC
   ```

### 4. 유틸리티 클래스

#### 📁 `NaverMapUrlGenerator.java`
**역할**: 네이버 지도 URL 생성

**주요 기능**:
- 길찾기 URL 생성
- 위치 URL 생성
- BigDecimal ↔ double 변환

---

## 🎨 프론트엔드 구조 분석

### 1. 메인 페이지 컴포넌트

#### 📁 `MapSearchPage.vue`
**역할**: 지도 검색 페이지의 전체 UI 및 로직 관리

**주요 섹션**:
```vue
<template>
  <!-- 필터 영역 -->
  <div class="filter-section">
    - 반경 선택 (3km, 5km, 10km, 20km)
    - 직무 필터 (프론트엔드, 백엔드, ...)
    - 검색어 입력
  </div>
  
  <!-- 좌측: 프로젝트 리스트 -->
  <div class="col-md-4">
    - 프로젝트 카드들
    - 로딩 상태
    - 빈 상태 처리
  </div>
  
  <!-- 우측: 지도 -->
  <div class="col-md-8">
    - 네이버 지도 이미지
    - 사용자 마커 (파란색)
    - 프로젝트 마커들 (빨간색)
    - 줌 컨트롤
  </div>
</template>
```

### 2. 상태 관리

**반응형 데이터**:
```javascript
const filters = ref({           // 필터 조건
  radius: '5',
  jobRole: '',
  keyword: ''
})

const userLocation = ref({      // 사용자 위치
  latitude: 37.5665,
  longitude: 126.9780,
  address: '위치 정보를 가져오는 중...'
})

const projects = ref([])        // 프로젝트 리스트
const mapImageUrl = ref('')     // 지도 이미지 URL
const mapZoom = ref(13)         // 줌 레벨
const visibleProjects = ref([]) // 화면에 표시될 프로젝트들
```

### 3. 핵심 함수들

#### 위치 정보 가져오기
```javascript
const getUserLocation = () => {
  // 1. 사용자 등록 주소 API 호출
  // 2. 실패 시 브라우저 위치 정보 사용
  // 3. 최종 실패 시 기본값 (서울시청)
}
```

#### 프로젝트 검색
```javascript
const applyFilters = async () => {
  // 1. API 호출
  // 2. 응답 데이터 저장
  // 3. 지도 이미지 URL 생성
  // 4. 마커 위치 계산
}
```

#### 마커 위치 계산
```javascript
const getProjectMarkerStyle = (project) => {
  // 1. 위도/경도 차이 계산
  // 2. 줌 레벨에 따른 스케일 적용
  // 3. 픽셀 좌표로 변환
  // 4. 지도 내 범위로 제한
}
```

---

## 🔄 데이터 흐름 분석

### 1. 사용자 위치 조회 흐름
```
프론트엔드 → MapSearchController.getUserAddress() 
         → MapSearchService.getUserLocation()
         → MapSearchMapper.findUserAddress()
         → 데이터베이스 (TBL_USER_M + TBL_ADDRESS_S)
         → MapProjectDto 반환
         → JSON 응답
```

### 2. 프로젝트 검색 흐름
```
프론트엔드 → MapSearchController.searchProjectsSimple()
         → MapSearchService.searchProjectsByUserId()
         → MapSearchMapper.findProjectsWithinRadius()
         → 하버사인 공식으로 거리 계산
         → MapProjectDto 리스트 반환
         → MapProjectResponse 리스트로 변환
         → JSON 응답
```

### 3. 지도 이미지 생성 흐름
```
프론트엔드 → MapSearchController.getNaverStaticMap()
         → 네이버 Static Map API 호출
         → 성공: 이미지 바이트 배열 반환
         → 실패: fallback JSON 응답
```

---

## 🎯 핵심 기능별 상세 설명

### 1. 거리 계산 (하버사인 공식)

**수학적 원리**:
```sql
6371 * acos(
  cos(radians(#{userLat})) * cos(radians(a.latitude)) * 
  cos(radians(a.longitude) - radians(#{userLon})) + 
  sin(radians(#{userLat})) * sin(radians(a.latitude))
)
```

**구성 요소**:
- `6371`: 지구 반지름 (km)
- `acos`: 역코사인 함수
- `cos`, `sin`: 삼각함수
- `radians`: 도를 라디안으로 변환

**동작 원리**:
1. 두 지점의 위도/경도를 라디안으로 변환
2. 구면 삼각법을 사용하여 각도 계산
3. 지구 반지름을 곱하여 실제 거리(km) 계산

### 2. 네이버 Static Map API

**API 호출 구조**:
```java
String staticMapUrl = String.format(
    "https://maps.apigw.ntruss.com/map-static/v2/raster?w=%d&h=%d&center=%.6f,%.6f&level=%d&format=png&maptype=basic&X-NCP-APIGW-API-KEY-ID=%s",
    width, height, centerLon, centerLat, level, naverClientId
);
```

**파라미터 설명**:
- `w`, `h`: 이미지 크기 (픽셀)
- `center`: 지도 중심 좌표
- `level`: 줌 레벨 (1-20)
- `format`: 이미지 형식 (png, jpg)
- `maptype`: 지도 타입 (basic, satellite)

### 3. 마커 위치 계산 알고리즘

**프론트엔드 계산 로직**:
```javascript
const getProjectMarkerStyle = (project) => {
  // 1. 위도/경도 차이 계산
  const latDiff = project.latitude - userLocation.value.latitude
  const lngDiff = project.longitude - userLocation.value.longitude
  
  // 2. 줌 레벨에 따른 스케일
  const zoomScale = Math.pow(2, mapZoom.value - 16)
  
  // 3. 1도당 픽셀 수 계산
  const pixelsPerDegree = 50000 * zoomScale
  
  // 4. 지도 중심(50%, 50%) 기준으로 픽셀 좌표 계산
  const x = 50 + (lngDiff * pixelsPerDegree / mapWidth * 100)
  const y = 50 - (latDiff * pixelsPerDegree / mapHeight * 100)
  
  // 5. 지도 범위 내로 제한
  return {
    left: `${Math.max(5, Math.min(95, x))}%`,
    top: `${Math.max(5, Math.min(95, y))}%`,
    transform: 'translate(-50%, -100%)'
  }
}
```

**핵심 개념**:
- **지도 중심**: 항상 50%, 50% 위치
- **좌표계 변환**: 위도/경도 → 픽셀 좌표
- **줌 스케일링**: 줌 레벨에 따른 배율 조정
- **범위 제한**: 지도 영역 밖 마커 방지

---

## 🔌 API 엔드포인트 분석

### 1. 사용자 주소 조회
```
GET /api/map/user-address?userId={userId}
```
**응답 예시**:
```json
{
  "latitude": 37.5665,
  "longitude": 126.9780,
  "address": "서울특별시 중구 세종대로 110"
}
```

### 2. 프로젝트 검색
```
GET /api/map/search?userId={userId}&radius={radius}&jobType={jobType}&keyword={keyword}&page={page}&size={size}
```
**응답 예시**:
```json
{
  "projects": [
    {
      "projectSq": 1,
      "projectTitle": "웹 개발 프로젝트",
      "companyName": "테크회사",
      "latitude": 37.5665,
      "longitude": 126.9780,
      "distance": 2.3,
      "naverMapUrl": "https://map.naver.com/v5/directions/..."
    }
  ],
  "totalCount": 15,
  "currentPage": 0,
  "totalPages": 1
}
```

### 3. 네이버 Static Map
```
GET /api/map/naver/static?centerLon={lon}&centerLat={lat}&width={width}&height={height}&level={level}
```
**성공 시**: PNG 이미지 바이트 배열
**실패 시**: JSON 응답
```json
{
  "success": false,
  "message": "지도 서비스를 일시적으로 사용할 수 없습니다",
  "location": {"longitude": 126.9780, "latitude": 37.5665},
  "fallback": true
}
```

---

## 🗄️ 데이터베이스 쿼리 분석

### 1. 테이블 구조
```sql
TBL_USER_M          -- 사용자 정보
├── user_sq (PK)
├── address_sq (FK)
└── user_is_deleted_yn

TBL_ADDRESS_S       -- 주소 정보
├── address_sq (PK)
├── latitude
├── longitude
├── address
└── detail_address

TBL_PROJECT_M       -- 프로젝트 정보
├── project_sq (PK)
├── company_sq (FK)
├── address_sq (FK)
├── project_ttl
└── project_recruit_end_dt

TBL_COMPANY_S       -- 기업 정보
├── company_sq (PK)
└── company_nm
```

### 2. 핵심 쿼리 분석

#### 사용자 주소 조회
```sql
SELECT a.latitude, a.longitude, a.address, a.detail_address, a.sigungu
FROM TBL_USER_M u
INNER JOIN TBL_ADDRESS_S a ON u.address_sq = a.address_sq
WHERE u.user_sq = #{userId} AND u.user_is_deleted_yn = 'N'
```

#### 반경 내 프로젝트 검색
```sql
SELECT 
  p.project_sq,
  p.project_ttl,
  c.company_nm,
  a.address,
  a.latitude,
  a.longitude,
  -- 하버사인 공식으로 거리 계산
  ROUND(6371 * acos(
    cos(radians(#{userLat})) * cos(radians(a.latitude)) * 
    cos(radians(a.longitude) - radians(#{userLon})) + 
    sin(radians(#{userLat})) * sin(radians(a.latitude))
  ), 1) as distance
FROM TBL_PROJECT_M p
INNER JOIN TBL_ADDRESS_S a ON p.address_sq = a.address_sq
INNER JOIN TBL_COMPANY_S c ON p.company_sq = c.company_sq
WHERE p.project_is_deleted_yn = 'N'
  AND p.project_recruit_end_dt >= CURDATE()
  AND (거리계산결과) <= #{radius}
ORDER BY distance ASC
LIMIT #{offset}, #{limit}
```

---

## 🗺️ 지도 표시 원리

### 1. 네이버 Static Map API 활용
- **정적 지도 이미지**: 실시간 지도 대신 이미지로 표시
- **마커 오버레이**: CSS로 마커를 지도 위에 배치
- **줌 컨트롤**: 줌 레벨 변경으로 지도 스케일 조정

### 2. 마커 시스템
```css
/* 사용자 마커 (파란색) */
.user-marker {
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -100%);
  animation: pulse 2s infinite;
}

/* 프로젝트 마커 (빨간색) */
.project-marker {
  position: absolute;
  cursor: pointer;
  transition: all 0.2s ease;
}
```

### 3. 좌표 변환 시스템
```
위도/경도 (WGS84) → 픽셀 좌표 → CSS 위치 (%)
```

---

## ⚡ 성능 최적화 전략

### 1. 데이터베이스 최적화
- **인덱스**: 위도/경도 컬럼에 공간 인덱스 적용
- **쿼리 최적화**: 하버사인 공식 계산을 DB에서 수행
- **페이징**: LIMIT/OFFSET으로 메모리 사용량 제한

### 2. API 최적화
- **캐싱**: 네이버 Static Map 이미지 캐싱
- **비동기 처리**: 지도 이미지 로딩과 데이터 조회 분리
- **에러 처리**: fallback 메커니즘으로 사용자 경험 보장

### 3. 프론트엔드 최적화
- **가상 스크롤링**: 대량 프로젝트 리스트 처리
- **디바운싱**: 검색어 입력 시 API 호출 최적화
- **이미지 지연 로딩**: 지도 이미지 lazy loading

---

## 🚀 확장 가능성

### 1. 추가 기능
- **실시간 지도**: 네이버 지도 JavaScript API 연동
- **클러스터링**: 많은 마커를 그룹화하여 표시
- **필터링**: 날짜, 급여, 경력 등 추가 필터

### 2. 성능 개선
- **Redis 캐싱**: 검색 결과 캐싱
- **CDN 활용**: 지도 이미지 CDN 배포
- **WebSocket**: 실시간 프로젝트 업데이트

### 3. 모바일 최적화
- **터치 제스처**: 핀치 줌, 드래그 지원
- **반응형 디자인**: 모바일 화면 최적화
- **PWA**: 오프라인 지도 캐싱

---

## 📝 결론

이 지도 기능은 **위치 기반 프로젝트 검색**을 위한 종합적인 솔루션으로, 다음과 같은 특징을 가집니다:

1. **정확한 거리 계산**: 하버사인 공식을 사용한 정밀한 거리 측정
2. **사용자 친화적 UI**: 직관적인 필터링과 마커 시스템
3. **확장 가능한 아키텍처**: 모듈화된 구조로 기능 추가 용이
4. **성능 최적화**: 데이터베이스와 API 레벨의 최적화 적용
5. **에러 처리**: 견고한 fallback 메커니즘

이 시스템은 프리랜서와 기업을 연결하는 핵심 기능으로, **위치 기반 매칭**을 통해 더 정확하고 효율적인 프로젝트 검색을 제공합니다.
